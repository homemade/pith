# TestProtectorScenarios

_Generated by `TestProtectorScenarios` via [sequencerec](https://github.com/joineduptech/doc/tree/main/sequencerec). Regenerated on every `go test` run — do not edit by hand._

## first send proceeds and is recorded

```mermaid
sequenceDiagram
    actor C as Caller
    participant SendState
    participant Debounce
    participant Quota

    Note over C: Check(content=hash-A, target=act-1:contact-1)
    C->>SendState: ReadEntry("act-1:contact-1")
    SendState-->>C: "entry", nil
    C->>Debounce: ShouldDefer()
    Debounce-->>C: false
    C->>Quota: ShouldDefer()
    Quota-->>C: false
    C->>SendState: RaisePeaks("act-1:contact-1", "map[at cap:1 leading-edge debounce window:1]")
    SendState-->>C: nil
    Note over C: → Proceed
    Note over C: send to downstream succeeded → RecordAsSent
    C->>SendState: RecordAsSent("act-1:contact-1", "hash-A")
    SendState-->>C: nil
```

## duplicate content to the same target is deferred

```mermaid
sequenceDiagram
    actor C as Caller
    participant SendState

    Note over C: Check(content=hash-A, target=act-1:contact-1)
    C->>SendState: ReadEntry("act-1:contact-1")
    SendState-->>C: "entry", nil
    Note over C: → Deferred (duplicate content)
```

## same-target follow-up within debounce window is deferred

```mermaid
sequenceDiagram
    actor C as Caller
    participant SendState
    participant Debounce

    Note over C: Check(content=hash-B, target=act-1:contact-1) — 1 send within debounce window
    C->>SendState: ReadEntry("act-1:contact-1")
    SendState-->>C: "entry", nil
    C->>Debounce: ShouldDefer()
    Debounce-->>C: true
    C->>SendState: RecordAsDeferred("act-1:contact-1", "<10 bytes>")
    SendState-->>C: nil
    Note over C: → Deferred (leading-edge debounce window) — deferred breadcrumb stamped
```

## deferred at quota cap with breadcrumb stamped

```mermaid
sequenceDiagram
    actor C as Caller
    participant SendState
    participant Debounce
    participant Quota

    Note over C: Check(content=hash-C, target=act-1:contact-3) — quota at hardCap=2, debounce window expired
    C->>SendState: ReadEntry("act-1:contact-3")
    SendState-->>C: "entry", nil
    C->>Debounce: ShouldDefer()
    Debounce-->>C: false
    C->>Quota: ShouldDefer()
    Quota-->>C: true
    C->>SendState: RecordAsDeferred("act-1:contact-3", "<10 bytes>")
    SendState-->>C: nil
    Note over C: → Deferred (at cap) — deferred breadcrumb stamped
```

## below-cap send proceeds; RecordAsSent appends to sendstate

```mermaid
sequenceDiagram
    actor C as Caller
    participant SendState
    participant Debounce
    participant Quota

    Note over C: Check(content=hash-D, target=act-1:contact-4) — counts start at 0
    C->>SendState: ReadEntry("act-1:contact-4")
    SendState-->>C: "entry", nil
    C->>Debounce: ShouldDefer()
    Debounce-->>C: false
    C->>Quota: ShouldDefer()
    Quota-->>C: false
    C->>SendState: RaisePeaks("act-1:contact-4", "map[at cap:1 leading-edge debounce window:1]")
    SendState-->>C: nil
    Note over C: → Proceed
    C->>SendState: RecordAsSent("act-1:contact-4", "hash-D")
    SendState-->>C: nil
```

